export interface Files {        // Identifiant unique du fichier (optionnel lors de la création)
  nom: string;         // Nom du fichier
  type: string;        // Type de fichier (ex. image/png)
  idFile: string;      // Identifiant de fichier
}
